import styled from 'styled-components';

export const Wrapper = styled.div`
  display: flex;
  column-gap: 1rem;
  margin: 7rem 10rem;
  flex-wrap: wrap;
  row-gap: 1rem;
`

export const Balance = styled.div`
  position: absolute;
  top: -2rem;
  left: 10rem;
`
